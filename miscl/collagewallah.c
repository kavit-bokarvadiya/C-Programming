#include <stdio.h>
int main() //question: take integer as input and print half of the nember.
{
    /*int a;
    printf("enter a number: ");
    scanf("%d", &a);
    float b , c;
    b = a;
    c = b / 2.0;
    printf("your half number is: %f",c);
    return 0;*/

    
    /*double a, c; // (take float input) and (print the fractional part of real number.)
    printf("enter a real number: ");
    scanf("%lf",&a);
    int b;
    b = a;
    c = a - b;
    printf ("your value is: %lf", c);
    return 0;*/

    /*double a, b, c, d;
    printf("enter a value of a:");
    scanf("%lf",&a); //hierarchy operator(preferencence)
    printf("enter a value of b:");
    scanf("%lf",&b);
    printf("enter a value of c:");
    scanf("%lf",&c);
    d = (a / b) * c + (a * c);
    printf("your ans is: %lf", d);
    return 0;*/

    char ch = 'A';
    printf("%d", ch);
    return 0;
}
