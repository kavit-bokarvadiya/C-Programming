#include<stdio.h>
int main()
{
    /*int physics, chemistry, maths, total, percentage;
    printf("enter a marks of physics: ");
    scanf("%d", &physics);
    printf("enter a marks of chemistry: ");
    scanf("%d", &chemistry);
    printf("enter a marks of maths: ");
    scanf("%d", &maths);
    total = physics + chemistry + maths;
    percentage = 100.0 * total / 300.0;
    printf("your percentage is: %d\n", percentage);
    if (percentage>35)
    {
        printf("pass");
    }
    else
    {
        printf("fail");
    }
    return 0;*/

    /*int a;  //any number whish is divisible by 2 is called even number
    printf("enter a value of a: ");
    scanf("%d",&a);
    if (a % 2 == 0)
    {
        printf("even");
    }
    else
    {
        printf("odd");
    }
    return 0;*/

    //question: any number whish is divisible by 2 is called even number(2 blocks of if statement)

    /*int a; //dabba ban gaya
    printf("enter a value of a: ");
    scanf("%d",&a); // dabbe me value dal di, a = 7
    if (a % 2 == 0)
    {
        printf("even"); // ignore
    }
    if (a % 2 != 0) // run odd condition
    {
        printf("odd"); // that is run
    }
    return 0;*/

    //question: Take positive integer input and tell if it is divisible by 5 or not.

    /*int a;
    printf("enter a number: ");
    scanf("%d", &a);
    if (a % 5 == 0)
    {
        printf("yes divisible by 5");
    }
    else
    {
        printf("no it's not divisible by 5");
    }
    return 0;*/
}