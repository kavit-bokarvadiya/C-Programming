#include <stdio.h>
#include <stdbool.h>

int main() {
    // Declare a boolean variable
    bool isRaining = true;

    // Check if it's raining
    if (isRaining) {
        printf("It's raining outside.\n");
    } else {
        printf("It's not raining.\n");
    }

    return 0;
}

